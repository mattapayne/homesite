<%= begin_rounded_box %>
<h2>My <%= link_to("Tumblr", "http://mpayne.tumblr.com") %> Posts</h2>
<% if posts && posts.not_empty? %>
  <% for post in posts %>
    <div class="tumblr_post">
      <%= link_to(post.title, post.url) %> (<%= post.date %>)
    </div>
  <% end %>
<% else %>
  <p>There are currently no Tumblr posts.</p>
<% end %>
<%= end_rounded_box %>
